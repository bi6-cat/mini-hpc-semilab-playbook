---
- name: Phase 1 placeholder for LDAP client role
  ansible.builtin.debug:
    msg:
      - "LDAP client role scaffold is active on {{ inventory_hostname }}."
      - "Phase 2 will configure SSSD/PAM/SSH for LDAP-only auth."
